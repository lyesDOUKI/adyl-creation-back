email:
  enabled: ${MAILING_ENABLED:false}
  from: ${MAIL_FROM}

  admin:
    address: ${ADMIN_EMAIL}

  templates:
    order-created:
      subject: "Nouvelle commande !"
      template: "emails/order-created.ftl"