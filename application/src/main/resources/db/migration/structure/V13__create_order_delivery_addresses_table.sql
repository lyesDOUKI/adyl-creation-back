CREATE TABLE ORDER_DELIVERY_ADDRESSES (
                                          id      UUID PRIMARY KEY,
                                          address VARCHAR(255) NOT NULL,
                                          city    VARCHAR(50) NOT NULL
);